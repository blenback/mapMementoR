# Parse GPX file and extract track points

Parse GPX file and extract track points

## Usage

``` r
parse_gpx(gpx_file)
```

## Arguments

- gpx_file:

  Path to the GPX file

## Value

Data frame with latitude, longitude, elevation, and point order
