# Get max/min distance and elevation from multiple GPX files

Get max/min distance and elevation from multiple GPX files

## Usage

``` r
get_track_stats(gpx_dir)
```

## Arguments

- gpx_dir:

  Directory containing GPX files

## Value

List with min and max distance and elevation
