% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multitrack_map_series.R
\name{multitrack_map_series}
\alias{multitrack_map_series}
\title{Loop over multiple track sets and styles to create multitrack maps}
\usage{
multitrack_map_series(
  output_dir = "outputs",
  styles = c("Dark"),
  custom_styles = NULL,
  track_sets_path = "track_sets.yaml",
  cache_data = TRUE,
  dpi = 300,
  page_size = "A4",
  base_size = 12,
  orientation = "portrait",
  with_elevation = TRUE,
  with_labels = FALSE,
  label_spacing = 0.5,
  label_size = 4,
  with_OSM = TRUE,
  with_hillshade = FALSE,
  components = c("highways", "streets", "water", "coast"),
  fade_directions = c("top", "bottom"),
  crop_shape = NULL,
  track_color_method = "hue_shift"
)
}
\arguments{
\item{output_dir}{Directory to save maps}

\item{styles}{Character vector of style names to use (built-in or custom) see \code{.mapMementoR_builtin_styles} in R/styles.R for built-in styles}

\item{custom_styles}{Optional named list of custom styles, each a list of color settings (route_color, bg_color, street_color, highway_color, water_color)}

\item{track_sets_path}{Path to track sets YAML file}

\item{cache_data}{Whether to cache OSM and hillshade data}

\item{dpi}{Image resolution in dots per inch}

\item{page_size}{Page size (e.g., "A5", "A4")}

\item{base_size}{Base font size for map text}

\item{orientation}{Page orientation (e.g., "portrait", "landscape")}

\item{with_elevation}{Boolean to include elevation charts}

\item{with_labels}{Boolean to include text labels along routes}

\item{label_spacing}{Spacing for labels along the path}

\item{label_size}{Size of route labels}

\item{with_OSM}{Boolean to include OSM background features}

\item{with_hillshade}{Boolean to include hillshade (elevation relief) background}

\item{components}{Character vector specifying which OSM components to include. Any combination of 'highways', 'streets', 'water', 'coast'. Defaults to all.}

\item{fade_directions}{Character vector specifying which sides to apply fade gradients to. Any combination of 'top', 'bottom', 'left', 'right'. Defaults to c('top', 'bottom').}

\item{crop_shape}{Optional shape to crop map to. Options: "circle", "ellipse". If NULL, no cropping is applied.}

\item{track_color_method}{Method for generating track colors when not specified. Options:
\itemize{
\item "hue_shift": Varies hue while maintaining similar saturation/lightness (default)
\item "complementary": Creates complementary and analogous colors
\item "gradient": Creates a gradient with varying lightness and saturation
}}
}
\value{
Saves maps for all track sets and styles
}
\description{
This function processes multiple track sets (groups of GPX files) and generates
maps with different visual styles. If route colors are not specified in the YAML
configuration, it automatically generates visually distinct colors using the
generate_track_colors() function based on each style's route_color.
}
\examples{
multitrack_map_series(
  output_dir = "outputs",
  styles = c("Dark", "Emerald"),
  custom_styles = list(MyStyle = list(route_color = "#123456", ...)),
  track_sets_path = "data-raw/track_sets.yaml",
  dpi = 300,
  page_size = "A4",
  base_size = 12,
  orientation = "portrait",
  with_elevation = TRUE,
  with_labels = FALSE,
  with_OSM = TRUE,
  with_hillshade = FALSE,
  cache_data = TRUE,
  components = c("highways", "streets", "water", "coast")
)
}
