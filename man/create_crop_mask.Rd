% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_crop_mask.R
\name{create_crop_mask}
\alias{create_crop_mask}
\title{Create a crop mask for map shapes}
\usage{
create_crop_mask(bbox, crop_shape, bg_color)
}
\arguments{
\item{bbox}{Bounding box as a numeric vector c(xmin, ymin, xmax, ymax)}

\item{crop_shape}{Shape to crop to. Options: "circle", "ellipse"}

\item{bg_color}{Background color for the mask (should match plot background)}
}
\value{
A list with 'mask' (sf polygon object) and 'adjusted_bbox' (bbox, unchanged)
}
\description{
This function generates a mask polygon that can be overlaid on a map to crop it to various shapes (circle, ellipse, etc.). The mask is a polygon with a hole in the desired shape.
}
\examples{
bbox <- c(-0.1, 51.5, 0.1, 51.6)
result <- create_crop_mask(bbox, "circle", "#0a0e27")
}
